This should all be set up ready to run if you give it the paths to the BLT data.
The main code is in "T_Chain_Chi_Horiz ..." which runs everything for a mooring.
This code calls the "Calc_Chi_TChain_2" to do the actual calculation for each data subset.
